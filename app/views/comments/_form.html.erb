<%= simple_form_for ([@movie, @movie.comments.build]) do |f| %>

    <% if @movie.comments.build.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this review from being saved:</h2>

          <ul>
            <% @comment.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%= f.input :comment, input_html: { maxlength: 250 } %>
    <%= f.button :submit %>
<% end %>